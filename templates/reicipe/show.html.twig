{% extends 'base.html.twig' %}

{% block title "Recette : " ~ slug %}
{# {% block title %}Toutes les recettes!
{% endblock %} #}

{% block body %}
	{{ slug }}
	{{demo}}
	{{person.name}}
	{{person.name | upper ~ " " ~ person.age }}
{% endblock %}
