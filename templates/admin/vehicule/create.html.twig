{% extends 'admin.html.twig' %}

{% block title "Creer un nouveau vehicule "  %}


{% block body %}
	<h1>Creer un nouveau vehicule</h1>

	{{ form_start(form) }}
	<div class="d-flex flex-column">
		{{ form_row(form.marque) }}
		{{ form_row(form.modele) }}
	</div>
	{{ form_rest(form) }}
	{{ form_end(form) }}
{% endblock %}
